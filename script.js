function mostrarMensagem() {
  const mensagem = `Não! Não tem como fugir;
Não! Põe na história o seu dom, vai;
É a hora de ir...`;

  document.getElementById("mensagem").innerText = mensagem;
}
